export default function JoinPlaceholderPage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-md flex-col justify-center gap-3 px-6">
      <h1 className="text-2xl font-semibold tracking-tight">Student join</h1>
      <p className="text-sm text-zinc-600">
        Check-in flow arrives in the next milestone (session + QR).
      </p>
    </main>
  );
}
