# v0.2 — Remove vs overhaul inventory

Source of cleanup truth for implementation (P1–P2). Keep code aligned with cloud + tripod station.

## Remove entirely

| Item | Location (approx.) | Notes |
|------|-------------------|--------|
| Student board camera UI | `app/join/join-qr-scanner.tsx` | Do not leave on `/join` |
| Projector check-in QR page | `app/teacher/.../projector/page.tsx` | Replace with presence board or delete |
| Rotating session QR API | `app/api/sessions/[sessionId]/qr/route.ts` | |
| Public finalize with board token | `POST /api/check-in` as student-facing mark | Replace with teacher `.../scan` |
| Join actions requiring projector token | `app/join/actions.ts` (board token path) | |
| Typed fallback under projector | Join + projector UI copy | |
| AP / LAN class runbook steps | old `10-classroom-runbook` AP sections | Rewritten |
| mkcert classroom ops | `scripts/setup-local-https.sh`, `scripts/start-https.js`, `certs/` as required | Optional local-dev only; not class day |
| LAN QR bootstrap as product | `scripts/dev-with-qr.js` classroom LAN URL | Dev helper only or remove |
| `QR_ROTATE_SECONDS` as student board rotate | env / docs | Replace with personal token TTL if needed |
| Product locks forbidding teacher-scan / requiring AP | old `project.mdc`, `AGENTS.md`, brief | Updated in P0 |

## Overhaul

| Item | From | To |
|------|------|-----|
| Hosting | Laptop + AP | Cloudflare + domain + always-on host |
| DB prod | SQLite file on laptop | Cloud DB |
| `/join` | Scan board / enter token | Show personal QR; wait for station |
| Teacher CTA | Open projector QR | Open **Station Scan** |
| `html5-qrcode` | Student join | **Teacher** Scan page only |
| Projector | Check-in QR + names | Presence board names/counts only |
| `source` | `qr` | `teacher_scan` (+ `manual` / `auto`) |
| README / smoke | LAN + mkcert | `https://<domain>` |

## Keep

Teacher PIN, import, roster, start/end session, late scoring, manual override, Excel export, demo INF191, unit tests (adapt smoke base URL).

## Abandoned product goals

- No-internet / captive AP classroom
- Class-wide certificate install
- Student self-scan of rotating projector QR as primary path
